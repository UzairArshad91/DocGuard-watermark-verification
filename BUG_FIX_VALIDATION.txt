================================================================================
DOCGUARD BUG FIX VALIDATION CHECKLIST
================================================================================

This document tracks the 9 bugs fixed in the DocGuard watermark verification system.

ISSUE #1: NO LIGHT THEME after logging in
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Added theme toggle button (☀ for dark mode, 🌙 for light mode) to EmployeeWindow header
- Added theme toggle button (☀ for dark mode, 🌙 for light mode) to AdminWindow header
- Modified login_gui.py to pass light_style and dark_style parameters to role windows
- Added toggle_theme() method to both EmployeeWindow and AdminWindow classes
- Theme is now preserved when switching between windows

Test Steps:
1. Run launcher.py
2. Log in as admin or employee
3. Click the theme toggle button (☀ or 🌙) in the header
4. Verify UI switches between light and dark themes
5. Verify all text and buttons are visible in both themes

Files Modified: login_gui.py, employee_gui.py, admin_gui.py


ISSUE #2: Admin close dialog Cancel button not working
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Added explicit .clicked.connect(self.reject) to the Cancel button in CloseDialog
- Cancel button now properly closes the dialog instead of being non-functional

Test Steps:
1. Run launcher.py
2. Log in as admin
3. Close the admin window (should prompt for verification)
4. Click Cancel button in the close dialog
5. Verify dialog closes and you return to the admin workspace

Files Modified: login_gui.py (CloseDialog class)


ISSUE #3: Send document error (AWAITING DETAILS)
================================================================================
STATUS: ⏳ PENDING

This issue requires the specific error message from the user.
When error details are provided, the fix will be applied to:
- employee.py (send_document function)
- employee_gui.py (send method)
- admin_gui.py (send_mail method)

The error could be related to:
- HTTP response validation (Flask endpoint not responding correctly)
- File path or recipient validation
- PIN verification
- Database connectivity
- Network/encryption issues

User Action Required: Provide the error message when attempting to send a document


ISSUE #4: Add user window goes "not responding" after first use
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Improved clear_content() method to properly disconnect and destroy nested widgets
- Added explicit setParent(None) and deleteLater() calls
- Added try-except block in submit() function for better error handling
- Ensured radio button group and all form fields are properly cleaned up

Test Steps:
1. Run: python add_user_gui.py
2. Click "Add user" button
3. Fill in name, email, PIN fields
4. Click "Add user" button to submit
5. Verify success message appears
6. Click "Back to menu"
7. Click "Add user" button AGAIN
8. Verify UI loads normally without "not responding" hang
9. Repeat steps 3-8 multiple times

Files Modified: add_user_gui.py


ISSUE #5: GUI mixing when switching Add User → Delete User
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Enhanced clear_content() method to recursively clean ALL nested layouts
- Now properly removes widgets from nested QFormLayout, QHBoxLayout, etc.
- Calls setParent(None) before deleteLater() for complete widget disconnection

Test Steps:
1. Run: python add_user_gui.py
2. Click "Add user" - verify form fields appear
3. Click "Back to menu"
4. Click "Delete user" - verify ONLY delete form appears (no add form fields)
5. Click "Back to menu"
6. Click "Add user" - verify ONLY add form appears (no delete form fields)
7. Repeat switching multiple times and verify no field overlap

Files Modified: add_user_gui.py


ISSUE #6: Change PIN notification doesn't auto-clear after 2 seconds
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Added status_timer (QTimer) to change_pin() dialog
- All status messages now start the 2-second timer
- Timer automatically clears status text when it expires

Test Steps:
1. Run launcher.py
2. Log in as admin
3. Click "Change PIN"
4. Enter a user ID and new PIN
5. Click "Update PIN"
6. Observe "PIN updated" message appears
7. Wait 2 seconds and verify message automatically disappears
8. Try invalid user ID and verify error message also auto-clears

Files Modified: admin_gui.py


ISSUE #7: Add recipient allows duplicate emails
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Added validation check in add() function
- Before adding email, system checks if it already exists in VERIFIED_RECIPIENTS
- If duplicate found, shows warning dialog to user

Test Steps:
1. Run launcher.py
2. Log in as admin
3. Click "Manage verified recipients"
4. Type an email that already exists in the list
5. Click "Add recipient"
6. Verify warning dialog appears: "[email] is already in the recipients list"
7. Click OK to close dialog
8. Verify email was NOT added twice

Files Modified: admin_gui.py


ISSUE #8: Delete recipient doesn't indicate when not found
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Added validation check in delete() function
- Before deleting, system checks if email exists in VERIFIED_RECIPIENTS
- If email not found, shows warning dialog to user

Test Steps:
1. Run launcher.py
2. Log in as admin
3. Click "Manage verified recipients"
4. Type a non-existent email address
5. Click "Delete recipient"
6. Verify warning dialog appears: "[email] not found in recipients list"
7. Click OK to close dialog
8. Verify nothing was deleted

Files Modified: admin_gui.py


ISSUE #9: Exported logs timestamp shows "#######"
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Modified export_logs() SQL query to JOIN Documents table
- Now extracts proper document filenames using os.path.basename()
- Exports timestamp column with proper spacing

Test Steps:
1. Run launcher.py
2. Log in as admin
3. Click "Export logs"
4. Observe "Exported to logs_export.csv" message
5. Open logs_export.csv in Excel or text editor
6. Verify timestamp column shows full date/time (not "#######")
7. Verify document column shows filenames (not IDs)

Files Modified: admin_gui.py


ISSUE #10: Exported logs doc_id shows user_id instead of document name
================================================================================
STATUS: ✅ FIXED

Changes Made:
- Modified export_logs() SQL query to JOIN Logs with Documents table
- Extracts filename from Documents table for each log entry
- Uses os.path.basename() to get just the filename without full path
- Column header changed from "doc_id" to "document"

Test Steps:
1. Run launcher.py
2. Log in as admin
3. Click "Export logs"
4. Observe "Exported to logs_export.csv" message
5. Open logs_export.csv in Excel or text editor
6. Verify column header shows "document" (not "doc_id")
7. Verify each row shows document filename (e.g., "report.docx")
   instead of numeric ID (e.g., "1")

Files Modified: admin_gui.py


================================================================================
SUMMARY OF CHANGES
================================================================================

Total Issues Fixed: 9/10 (1 pending user details)

Files Modified:
- login_gui.py: Theme passing, cancel button fix
- employee_gui.py: Theme support, toggle button
- admin_gui.py: Theme support, toggle button, 5 recipient/export fixes
- add_user_gui.py: Widget cleanup fixes

All changes have been validated:
✓ Python compilation successful for all files
✓ All imports successful
✓ No syntax errors detected

Next Step:
- Provide error message from Issue #3 (send document error) for final fix
- Run all test steps above to validate each fix
- Report any issues found during testing
